/// <reference types="cypress" />

describe("Test", () => {
  it("sample test1 ", () => {
    expect(true).to.equal(true);
  });

  it("sample test2", () => {
    expect(true).to.equal(true);
  });
});
